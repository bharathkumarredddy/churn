services:
  - type: web
    name: churn-predictor-fixed
    env: python
    runtime: python-3.10
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    plan: free
    autoDeploy: true
